// config.ts

// Ethereum network provider URL (replace with your own)
export const providerUrl: string = 'https://solana.io/v3/904128947128937821345';

// Smart contract address (replace with your contract's address)
export const contractAddress: string = '';

// Sender wallet address (replace with your wallet's address)
export const senderAddress: string = '';

// Private key for signing transactions (DO NOT expose in production code)
export const privateKey: string = '';
